<textarea id="{{ textarea.id }}" name="{{ textarea.name }}" {% if textarea.disabled %}disabled="disabled"{% endif %} class="{{ textarea.cssClass }}" style="{{ textarea.cssStyle }}" {{ textarea.dynamicAttributes|join(' ') }}>{{ textarea.value }}</textarea>