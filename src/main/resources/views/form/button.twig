<button {% if button.id %}id="{{ button.id }}"{% endif %} {% if button.name %}name="{{ button.name }}"{% endif %} type="submit" value="{{ button.value }}" {% if button.disabled %}disabled="disabled"{% endif %} class="{{ button.cssClass }}" style="{{ button.cssStyle }}" {{ button.dynamicAttributes|join(' ') }}>{{ button.label }}</button>