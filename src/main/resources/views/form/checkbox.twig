{% if checkbox.label %}<label>{% endif %}<input id="{{ checkbox.id }}" name="{{ checkbox.name }}" {% if checkbox.disabled %}disabled="disabled"{% endif %} type="checkbox" value="{{ checkbox.value }}" {% if checkbox.checked %}checked="checked"{% endif %} class="{{ button.cssClass }}" style="{{ button.cssStyle }}" {{ checkbox.dynamicAttributes|join(' ') }} />{% if checkbox.label %} {{ checkbox.label }}</label>{% endif %}