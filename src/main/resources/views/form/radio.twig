{% if radio.label %}<label>{% endif %}<input id="{{ radio.id }}" name="{{ radio.name }}" {% if radio.disabled %}disabled="disabled"{% endif %} type="radio" value="{{ radio.value }}" {% if radio.checked %}checked="checked"{% endif %} {{ radio.dynamicAttributes|join(' ') }} />{% if radio.label %} {{ radio.label }}</label>{% endif %}