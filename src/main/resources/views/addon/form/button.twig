<button {% if button.id %}id="{{ button.id }}"{% endif %} {% if button.name %}name="{{ button.name }}"{% endif %} type="submit" value="{{ button.value }}" {% if button.disabled %}disabled="disabled"{% endif %} {% if button.cssClass %}class="{{ button.cssClass }}"{% endif %} {% if button.cssStyle %}style="{{ button.cssStyle }}"{% endif %} {{ button.dynamicAttributes|join(' ') }}>{{ button.label }}</button>