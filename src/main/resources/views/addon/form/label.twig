<label for="{{ label.path }}" {% if label.cssClass %}class="{{ label.cssClass }}"{% endif %} {% if label.cssStyle %}style="{{ label.cssStyle }}"{% endif %} {{ label.dynamicAttributes|join(' ') }}>{{ label.label }}</label>